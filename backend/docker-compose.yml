services:
  backend:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - .:/app/backend
    env_file:
      - .env
    command: uvicorn backend.api:app --host 0.0.0.0 --port 8080 --reload
